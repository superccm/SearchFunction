最大资源网，http://www.zuidazy5.com/index.php?m=vod-search-pg-{PAGE}-wd-{KEYWORD}，javascript:function getSource(){var root = document.getElementsByClassName('xing_vb4');var source = '';for(i = 0;i<root.length;i++){item = root[i].getElementsByTagName('a');source = source + item[0].innerText+',http://'+window.location.host+item[0].getAttribute('href')+'\n'};return source;}getSource();，javascript:function confirmEnding(str, target){var start = str.length-target.length;var arr = str.substr(start,target.length);if(arr == target){return true;}return false;}function getSource(){var root = document.getElementsByClassName('vodplayinfo')[1].getElementsByTagName('li');var source = '';var temp = '';for(i = 0;i<root.length;i++){temp = root[i].innerText.replace('$',',');if(confirmEnding(temp,'.m3u8')){source = source +temp+'\n'}};return source;}getSource();
OK资源网，http://www.jisudhw.com/index.php?m=vod-search-pg-{PAGE}-wd-{KEYWORD}，javascript:function getSource(){var root = document.getElementsByClassName('xing_vb4');var source = '';for(i = 0;i<root.length;i++){item = root[i].getElementsByTagName('a');source = source + item[0].innerText+',http://'+window.location.host+item[0].getAttribute('href')+'\n'};return source;}getSource();，javascript:function confirmEnding(str, target){var start = str.length-target.length;var arr = str.substr(start,target.length);if(arr == target){return true;}return false;}function getSource(){var root = document.getElementsByClassName('vodplayinfo')[1].getElementsByTagName('li');var source = '';var temp = '';for(i = 0;i<root.length;i++){temp = root[i].innerText.replace('$',',');if(confirmEnding(temp,'.m3u8')){source = source +temp+'\n'}};return source;}getSource();
永久资源网，http://www.yongjiuzy1.com/index.php?m=vod-search-pg-{PAGE}-wd-{KEYWORD}，javascript:function getSource(){var root = document.getElementsByClassName('DianDian');var source = '';for(i = 0;i<root.length;i++){item = root[i].getElementsByTagName('a');source = source + item[0].innerText+',http://'+window.location.host+item[0].getAttribute('href')+'\n'};return source;}getSource();，javascript:function confirmEnding(str, target){var start = str.length-target.length;var arr = str.substr(start,target.length);if(arr == target){return true;}return false;}function getSource(){var root = document.getElementsByClassName('movievod')[0].getElementsByTagName('li');var source = '';var temp = '';for(i = 0;i<root.length;i++){temp = root[i].innerText.replace('$',',');if(confirmEnding(temp,'.m3u8')){source = source +temp+'\n'}};return source;}getSource();
